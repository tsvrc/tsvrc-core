<!-- Canonical agent instructions live in AGENTS.md, imported below, per Anthropic's
     documented pattern for repos that already have an AGENTS.md (Claude Code reads
     CLAUDE.md, not AGENTS.md, so this keeps both tools reading the same file instead of
     two files drifting apart). See AGENTS.md for the actual content. -->

@AGENTS.md
